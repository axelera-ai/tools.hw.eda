---
version: 2
updates:
  # Submodule updates are handled by .github/workflows/update-submodules.yml
  # which tracks upstream releases only (not every commit).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
